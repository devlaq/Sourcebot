import { Discord } from '../lib/deps.ts';

class DMODCommand extends Discord.Command {
    name = 'dmod';
    aliases = [];
    execute(context: Discord.CommandContext) {
        
    }
}

export default DMODCommand;